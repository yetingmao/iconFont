#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../ttf2svg/bin/ttf2svg.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../ttf2svg/bin/ttf2svg.js" "$@"
  ret=$?
fi
exit $ret
